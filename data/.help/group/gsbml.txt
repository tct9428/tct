📢 *Anti-Status Broadcast Manual*

This module detects and automatically deletes "Status Updates" that users forward into your group. It prevents the group from being cluttered with personal status posts.

_Note: The bot always deletes the status broadcast immediately. You can configure if it should punish the user afterwards._

⊷═══════════════════⊷

*1. Enable or Disable*
Turn the protection on or off for the current group.

`.gsbml on`
> • _Explanation:_ Activates the module. Status broadcasts will be deleted.

`.gsbml off`
> • _Explanation:_ Disables the module.

•═══════════════════•

*2. Punishment Modes*
Decide what happens to the user after their message is deleted.

`.gsbml mode warn`
> • _Explanation:_ The bot just warns the user. It will not kick them.

`.gsbml mode kick`
> • _Explanation:_ The bot counts violations. If the user exceeds the limit, they are *Kicked*.

⊷═══════════════════⊷

*3. Configuring Limits*
Set how many chances a user gets before being kicked (only applies in 'kick' mode).

`.gsbml maxwarns <number>`

> • *Example:* `.gsbml maxwarns 3`
> • _Explanation:_ The user gets 2 warnings. On the 3rd status broadcast, they are removed.

*Set Warning Auto-Delete (TTL)*
Make the bot's warning messages disappear after a while to keep chat clean.

`.gsbml ttl <minutes>`
> • *Example:* `.gsbml ttl 1`
> • _Explanation:_ The "⚠️ Warning" message deletes itself after 1 minute.

•═══════════════════•

*4. Customizing the Warning*
Change the text the bot sends. You can use `{user}` (mention), `{count}` (current warnings), and `{max}` (limit).

`.gsbml warnmsg <text>`

> • *Example:* `.gsbml warnmsg @{user}, please do not forward status updates here!`
> • _Action:_ Sends that text when a status is deleted.

*Resetting Warnings*
`.gsbml resetwarns @user`
> • _Explanation:_ Sets a specific user's violation count back to 0.
> • _Note:_ Running `.gsbml resetwarns` without a mention resets *everyone* in the group.

⊷═══════════════════⊷

*5. Managing Other Groups (Remote)*
Configure settings for other groups using extensions.

`.gsbml <command> jd:groupName`

> • *Example:* `.gsbml on jd:OfficialGroup`
> • _Explanation:_ Enables protection in "OfficialGroup".
> • *Example:* `.gsbml mode kick jd:all`
> • _Explanation:_ Sets the punishment mode to "Kick" for *all* groups in your JIDBook.

«««««««««««««« • »»»»»»»»»»»»»»
