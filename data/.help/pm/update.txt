🔄 *System Update Manual*

This module allows the bot to check for the latest source code updates from the official repository and apply them immediately. It ensures your bot stays up-to-date with new features and bug fixes.

_Note: This command is restricted to the Bot Owner and Sudo users._

✦═════════◆═════════✦

*1. Check for Updates*
View the current version installed and check if a new version is available online.

`.update` or `.update check`

> • _Output:_ Displays the current version, release date, and the "What's New" notes from the server.

✦═════════◆═════════✦

*2. Apply Update*
Download and install the latest changes via Git.

`.update now`

> • _Explanation:_ The bot performs a `git pull` to fetch the latest code and then restarts to apply changes.

✦═════════◆═════════✦

*3. Force Update (Reset)*
If the bot is stuck or the update fails due to local changes, this forces a re-download.

`.update force`

> • _Explanation:_ Performs a `git reset --hard` to discard any local changes and forces a clean pull from the server.
> • _Warning:_ Use this only if the normal update fails.

✦═════════◆═════════✦
