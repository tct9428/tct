🔄 *System Reboot Manual*

_This module allows the Bot Owner to restart the bot remotely._
_It is useful for applying updates, clearing memory, or refreshing connections without accessing the server terminal._

_⚠️ Warning: Only use this if your bot is running under a Process Manager (like PM2, Docker, or Systemd) that automatically restarts the app when it quits._

•════════◄►════════•

*1. Immediate Reboot*
Restart the bot instantly.

`.reboot` or `.rb`

> • _Explanation:_
> 1. The bot sends a "SYSTEM REBOOT" status box.
> 2. It shuts down immediately.
> 3. Once your server restarts it, the bot sends a "SYSTEM STATUS" confirmation message back to you to confirm it is back online.

•════════◄►════════•

*2. Scheduled Reboot*
Set a timer before the bot restarts.

`.reboot <seconds>`

> • *Example:* `.reboot 10`
> • _Explanation:_ The bot sends a warning and waits 10 seconds before shutting down.

•════════◄►════════•

*3. Critical Requirement*
> • *Process Manager:* The bot uses a self-termination signal (`SIGINT`) to stop itself. It completely relies on your server environment (PM2, Docker) to detect the stop and start it up again.
> • *Manual Mode:* If you run the bot manually (e.g., just `go run or ./,tctbot .`), this command will simply turn the bot off and it will not come back.

«««««««««««««« • »»»»»»»»»»»»»»
