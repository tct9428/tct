🔌 *Logout Manual*

This module allows you to remotely terminate the bot's WhatsApp session.
**Warning:** This action is destructive. It destroys the session file, meaning you will need to rescan the QR code to log the bot back in.

_Note: This command is restricted to the Bot Owner and Sudo users. It works in Private Messages (PM) only._

✦═════════◆═════════✦

*1. Initiating Logout*
Request to disconnect the bot.

`.logout`
> • _Action:_ The bot will send a "CRITICAL WARNING" message explaining the consequences.
> • _Status:_ The bot waits for explicit confirmation.

✦═════════◆═════════✦

*2. Confirming Disconnection*
Execute the logout process.

`.logout confirm`
> • _Action:_ The bot sends a final "SYSTEM LOGOUT" notice, disconnects from WhatsApp servers, deletes the local session, and shuts down the process.
> • _Result:_ You must physically access the terminal and rescan the QR code to restart.

✦═════════◆═════════✦

*3. Safety Checks*
> • *PM Only:* To prevent accidental logouts in groups, this command only works in private chats.
> • *Confirmation:* You must type `.logout confirm` (or `.logout yes`). The bot will not logout on the first command.

«««««««««««««« • »»»»»»»»»»»»»»
